# La configuration ne doit jamais être servie en clair, même si le serveur
# cessait d'interpréter le PHP.
<FilesMatch "^(config|config\.example)\.php$">
    Require all denied
</FilesMatch>
Options -Indexes
